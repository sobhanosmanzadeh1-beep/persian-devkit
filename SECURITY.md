# سیاست امنیتی

## نسخه‌های پشتیبانی‌شده

| نسخه | پشتیبانی |
|------|-----------|
| 2.x  | ✅ فعال |
| 1.x  | ⚠️ فقط رفع باگ‌های امنیتی |

## گزارش آسیب‌پذیری

اگر آسیب‌پذیری امنیتی پیدا کردید، **لطفاً issue عمومی باز نکنید**.

به‌جای آن به `sobhanosmanzadeh1@gmail.com` ایمیل بزنید با:

- نوع آسیب‌پذیری
- مراحل بازتولید
- تأثیر احتمالی
- نسخهٔ pdev

## زمان پاسخ

- **تأیید دریافت:** حداکثر ۴۸ ساعت
- **ارزیابی اولیه:** حداکثر ۷ روز
- **رفع مشکل:** ۱-۳۰ روز بسته به شدت

## دامنهٔ امنیت

دستورات مرتبط:
- `pdev security jwt-encode` / `jwt-decode`
- `pdev security encrypt` / `decrypt`
- `pdev security hash-password` / `verify-password`
- `pdev security secret`
- `pdev security cert-info`

## تشکر

از همهٔ کسانی که به امنیت این پروژه کمک می‌کنند، سپاسگزاریم.